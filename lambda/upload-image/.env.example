# Lambda 로컬 테스트용 환경 변수
# 실제 .env 파일을 생성하여 사용하세요 (gitignore 처리 필수)

AWS_REGION=ap-northeast-2
S3_BUCKET=ktb-3-community-images-dev
FRONTEND_URL=http://localhost:3000

# JWT_SECRET은 AWS Parameter Store에서 가져오므로 로컬에서는 직접 설정
# 운영 환경에서는 Parameter Store 사용: /community/JWT_SECRET
JWT_SECRET=your-local-jwt-secret-for-testing

# AWS 자격 증명 (로컬 테스트 시만 필요)
# ~/.aws/credentials 프로필을 사용하는 경우 아래는 불필요
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key